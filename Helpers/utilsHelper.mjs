import { json } from "body-parser";

export class UtilsHelper{
    constructor(){

    }
    convertToJson(jsObject){
        return JSON.stringify(item)
    }

    convertToJavaScript(jsonData){
        return JSON.parse(item)
    }
}